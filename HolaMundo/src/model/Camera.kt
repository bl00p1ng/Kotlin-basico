package model

class Camera {
    // Atributos
    var isOn: Boolean = false

    // Métodos
    fun turnOn() {
        isOn = true
    }

    fun turnOf() {
        isOn = false
    }
}